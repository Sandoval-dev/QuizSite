@{
    ViewData["Title"] = "Access Denied";
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Access Denied - Quiz Platform</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
    <style>
        :root {
            --primary-dark: #1a2a3a;
            --primary-light: #2d3e50;
            --accent-teal: #00c9b7;
            --accent-orange: #ff7b54;
            --accent-yellow: #ffd166;
            --text-light: #f8f9fa;
            --text-dark: #212529;
            --error-red: #ff4757;
        }

        body {
            background: linear-gradient(135deg, var(--primary-dark), var(--primary-light));
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Poppins', 'Segoe UI', sans-serif;
            color: var(--text-light);
            margin: 0;
            padding: 1rem;
            background-image: 
                radial-gradient(circle at 10% 20%, rgba(255, 71, 87, 0.1) 0%, transparent 20%),
                radial-gradient(circle at 90% 80%, rgba(0, 201, 183, 0.1) 0%, transparent 20%);
        }

        .access-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            color: var(--text-dark);
            border-radius: 20px;
            padding: 3rem 2.5rem;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2), 
                        0 5px 15px rgba(0, 0, 0, 0.1);
            text-align: center;
            max-width: 600px;
            width: 100%;
            border: 1px solid rgba(255, 255, 255, 0.3);
            position: relative;
            overflow: hidden;
            align-items: center;
            animation: fadeInUp 0.8s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        .access-card::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(
                to bottom right,
                transparent 0%,
                rgba(255, 71, 87, 0.05) 50%,
                transparent 100%
            );
            transform: rotate(30deg);
            z-index: -1;
        }

        .access-icon {
            color: var(--error-red);
            font-size: 5rem;
            margin-bottom: 1.5rem;
            position: relative;
            display: inline-block;
            animation: pulse 1.5s infinite, shake 0.8s ease-in-out;
        }

        .access-icon::after {
            content: '';
            position: absolute;
            top: -10px;
            left: -10px;
            right: -10px;
            bottom: -10px;
            border: 2px solid var(--error-red);
            border-radius: 50%;
            opacity: 0;
            animation: ripple 2s infinite;
        }

        h2 {
            font-weight: 700;
            margin-bottom: 1.5rem;
            color: var(--error-red);
            position: relative;
            display: inline-block;
        }

        h2::after {
            content: '';
            display: block;
            width: 80px;
            height: 4px;
            background: var(--error-red);
            margin: 0.5rem auto;
            border-radius: 2px;
            animation: stretch 1.5s ease-in-out infinite alternate;
        }

        .lead {
            font-size: 1.1rem;
            color: var(--primary-light);
            margin-bottom: 1.5rem;
            line-height: 1.6;
        }

        .text-muted {
            color: #6c757d !important;
            margin-bottom: 2rem;
            font-size: 0.95rem;
        }

        .btn-custom {
            background: linear-gradient(135deg, var(--accent-orange), #ff6347);
            border: none;
            color: white;
            font-weight: 600;
            padding: 0.75rem 2rem;
            border-radius: 8px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            font-size: 0.9rem;
            box-shadow: 0 4px 15px rgba(255, 123, 84, 0.3);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            display: inline-flex;
            align-items: center;
            justify-content: center;
        }

        .btn-custom:hover {
            background: linear-gradient(135deg, #ff6347, var(--accent-orange));
            transform: translateY(-3px);
            box-shadow: 0 6px 20px rgba(255, 123, 84, 0.4);
            color: white;
        }

        .btn-custom:active {
            transform: translateY(1px);
        }

        .btn-custom i {
            margin-right: 8px;
            font-size: 1rem;
        }

        @@keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @@keyframes pulse {
            0% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.1);
            }
            100% {
                transform: scale(1);
            }
        }

        @@keyframes shake {
            0%, 100% {
                transform: translateX(0);
            }
            20%, 60% {
                transform: translateX(-5px);
            }
            40%, 80% {
                transform: translateX(5px);
            }
        }

        @@keyframes ripple {
            0% {
                transform: scale(0.8);
                opacity: 0.5;
            }
            100% {
                transform: scale(1.4);
                opacity: 0;
            }
        }

        @@keyframes stretch {
            0% {
                transform: scaleX(0.8);
                opacity: 0.6;
            }
            100% {
                transform: scaleX(1.2);
                opacity: 1;
            }
        }

        /* Responsive adjustments */
        @@media (max-width: 576px) {
            .access-card {
                padding: 2rem 1.5rem;
            }
            
            .access-icon {
                font-size: 4rem;
            }
            
            h2 {
                font-size: 1.5rem;
            }
            
            .lead {
                font-size: 1rem;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-6">
                <div class="access-card animate__animated animate__fadeIn">
                    <div class="mb-4">
                        <i class="fas fa-ban access-icon animate__animated animate__headShake animate__infinite animate__slower"></i>
                    </div>
                    <h2 class="fw-bold mb-3">Erişim Reddedildi</h2>
                    <p class="lead">Bu sayfaya erişim için gerekli izinlere sahip değilsiniz.</p>
                    <p class="text-muted">Eğer bu bir hata olduğunu düşünüyorsanız, lütfen sistem yöneticinizle iletişime geçin.</p>
                    <a href="/" class="btn btn-custom mt-3 animate__animated animate__fadeInUp animate__delay-1s">
                        <i class="fas fa-home"></i> Anasayfaya Dön
                    </a>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Add slight delay to animations for better visual effect
        document.addEventListener('DOMContentLoaded', function() {
            const elements = document.querySelectorAll('.animate__animated');
            elements.forEach((el, index) => {
                if (index > 0) {
                    el.style.animationDelay = `${index * 0.2}s`;
                }
            });
        });
    </script>
</body>

</html>